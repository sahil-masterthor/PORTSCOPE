# PORTSCOPE
Portscope is a multithreaded Python TCP port scanner that detects open ports, identifies 60+ common services (SSH, HTTP, MySQL, Redis &amp; more), and grabs live banners. Results export as JSON and render in a sleek dark-mode dashboard with radar visuals, bento cards, GSAP animations &amp; drag-and-drop upload.
